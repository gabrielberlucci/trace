{
  "name": "@app/shared",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && tsc-alias -p tsconfig.json --resolve-full-paths",
    "dev": "concurrently \"tsc -w\" \"tsc-alias -w -p tsconfig.json --resolve-full-paths\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "tsc-alias": "^1.8.17"
  }
}
